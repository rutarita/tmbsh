def fib(n)
    num=$n->num()
    if $num->eq?(%1)
        return %1
    elif $num->eq?(%0)
        return %0
    end
    a=%0
    b=%1
    for i in $num
        c=$b->add($a)
        a=$b
        b=$c
    end
    return $a
end

for i in %1000
    echo $fib($i) # a bit of test
end
