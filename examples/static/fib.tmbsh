def fib(n)
    num=$n->float()
    if $num->eq?(%1.0)
        return %1.0
    elif $num->eq?(%0.0)
        return %0.0
    end
    a=%0.0
    b=%1.0
    for i in $num
        c=$b->add($a)
        a=$b
        b=$c
    end
    return $a
end

for i in %1000
    echo $fib($i) # a bit of test
end
